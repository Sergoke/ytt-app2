import { IsGuestDirective } from './is-guest.directive';

describe('IsGuestDirective', () => {
  it('should create an instance', () => {
    const directive = new IsGuestDirective();
    expect(directive).toBeTruthy();
  });
});
